{
  "name": "btp_experience_build_backend",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^7",
    "@sap/cds-hana": "^2",
    "@sap/xssec": "^3.6.1",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1",
    "@sap/cds-dk": "^7"
  },
  "scripts": {
    "start": "cds-serve",
    "start-local": "cds serve all --with-mocks --in-memory"
  },
  "cds": {
    "requires": {
      "db": "hana",
      "auth": {
        "kind": "mocked"
      }
    }
  }
}
